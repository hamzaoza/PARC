PAD.Clean = function(clinic) {
    
    // function dispose(mesh) {
    //     console.log(mesh);
    //     mesh.geometry.dispose();
    //     mesh.material.dispose();
    //     mesh = undefined;
    //     console.log(mesh);
    // }

    // function cleaner(clinic) {

    //     clinic.children.forEach(function(child){

    //         if (child.type == "Group") {
    //             cleaner(child);
    //         } else {
    //             dispose(child);
    //         }

    //     });

    // }

    // cleaner(clinic);

}